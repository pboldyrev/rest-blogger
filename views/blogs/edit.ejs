<%- include("../partials/header") %>

<div class="container">
  <div class="row justify-content-center">
    <form class="col-lg-6" action="/blogs/<%= blog.id %>?_method=PUT" method="POST">
      <h2 class="mb-4">Edit "<%= blog.title %>"</h2>
      <div class="form-group">
        <label for="title">Blog Title</label>
        <input id="title" name="title" class="form-control" value="<%= blog.title %>">
        <small class="form-text text-muted">What inspires you in the world?</small>
      </div>
      <div class="form-group">
        <label for="url">Image URL</label>
        <input id="url" name="url" class="form-control" value="<%= blog.image %>">
      </div>
      <div class="form-group">
        <label for="body">Blog Body</label>
        <textarea id="body" name="body" class="form-control" rows="4"><%= blog.body %></textarea>
      </div>
      <button type="submit" class="btn btn-primary">Update blog</button>
      <a href="/blogs/<%= blog._id %>" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
</div>

<%- include("../partials/footer") %>
